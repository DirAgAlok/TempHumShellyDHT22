
      payload = postRequest(serverName, auth_key, id);
      //lcd.clear();

#include "constructOutput.h";

      if (dhtError == 1)
      {
        delay(5000);
#include "constructOutput.h"
      }